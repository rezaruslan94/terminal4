<p id="notice"><%= notice %></p>

<h1>Terminal4updates</h1>

<table>
  <thead>
    <tr>
      <th>Qty</th>
      <th>Terminal fourth</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @terminal4updates.each do |terminal4update| %>
      <tr>
        <td><%= terminal4update.qty %></td>
        <td><%= terminal4update.terminal_fourth_id %></td>
        <td><%= link_to 'Show', terminal4update %></td>
        <td><%= link_to 'Edit', edit_terminal4update_path(terminal4update) %></td>
        <td><%= link_to 'Destroy', terminal4update, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Terminal4update', new_terminal4update_path %>
